#!/bin/bash

# Скрипт для быстрого обновления бота на Timeweb Cloud

echo "🔄 Начинаю обновление бота..."

# Остановка текущего процесса
echo "⏹ Останавливаю текущий процесс..."
pkill -f "python main.py" || pkill -f "python3 main.py" || echo "Процесс не найден"

# Переход в папку бота
cd ~/telegram_bot || cd /root/telegram_bot || {
    echo "❌ Папка с ботом не найдена!"
    echo "Укажите путь к папке бота:"
    read BOT_PATH
    cd "$BOT_PATH" || exit 1
}

# Обновление через git (если используется)
if [ -d ".git" ]; then
    echo "📥 Обновляю через Git..."
    git pull origin main
else
    echo "⚠️ Git не используется. Обновите файлы вручную!"
fi

# Установка зависимостей
echo "📦 Устанавливаю зависимости..."
pip install -r requirements.txt || pip3 install -r requirements.txt

# Проверка FFmpeg
if ! command -v ffmpeg &> /dev/null; then
    echo "⚠️ FFmpeg не установлен. Установите его для работы с аудио:"
    echo "   sudo apt install ffmpeg  # для Ubuntu/Debian"
    echo "   sudo yum install ffmpeg   # для CentOS/RHEL"
fi

# Запуск бота
echo "🚀 Запускаю бота..."
nohup python main.py > bot.log 2>&1 &

# Проверка запуска
sleep 2
if pgrep -f "python main.py" > /dev/null; then
    echo "✅ Бот успешно запущен!"
    echo "📋 Логи: tail -f bot.log"
else
    echo "❌ Ошибка при запуске. Проверьте логи:"
    echo "   tail -100 bot.log"
fi

